#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "polynomal"

Polynomal.configure do |config|
  config.host = "http://localhost/api/v1/metrics"
  config.port = "3000"
  config.max_queue_size = 50
end

require "pry"
Pry.start
